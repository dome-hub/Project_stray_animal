# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ไฟล์ความลับ — ห้าม push ขึ้น GitHub เด็ดขาด!
.env
.env.local
.env.production

# Python (ai_server)
ai_server/venv
ai_server/__pycache__
*.pyc

# รูปสำหรับ train โมเดลเพิ่ม (ไฟล์เยอะ/หนัก ไม่ควร push ขึ้น GitHub)
thai_ridgeback/
siberian_husky/
thai_bangkaew/
labrador_retriever/
golden_retriever/
khao_manee/
korat/
suphalak/
konja/
new_breeds/
domestic_shorthair/
calico/
tortoiseshell/
thai_mixed_breed_dog/
*.zip

# ไฟล์ประกอบการทำเล่มรายงาน (ไดอะแกรม ERD/DFD/Flowchart, ภาพหน้าจอแอป, ไฟล์ Word)
# ไม่ใช่ส่วนหนึ่งของโค้ดหรือการ deploy — และภาพหน้าจอบางรูปมีอีเมล/เบอร์โทรของผู้ใช้ทดสอบติดมาด้วย
# ซึ่งไม่ควรอยู่ใน repo สาธารณะ ให้เก็บไว้ในเครื่อง/สำรองขึ้น Google Drive แทน
docs/

# Impeccable design skill — ติดตั้งผ่าน `npx impeccable install`, เป็นเครื่องมือ dev เฉพาะเครื่อง ไม่ต้อง commit (ไฟล์เยอะมาก ~60k บรรทัด)
.claude/skills/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
